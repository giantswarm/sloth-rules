# This file is automatically generated. Any manual changes will be wiped out.
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: platform-atlas-kube-prometheus-stack-operator-availability
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: atlas
    ## Makes sure the SLO metrics are sent to grafana cloud
    giantswarm.io/remote-write-target: grafana-cloud
spec:
  service: "kube-prometheus-stack"
  labels:
    owner: "atlas"
  slos:
    - name: "operator-availability"
      objective: 95
      description: kube-prometheus-stack prometheus-operator is unavailable
      sli:
        events:
          errorQuery: |-
            {{`count_over_time(up{job=~"kube-prometheus-stack-operator|prometheus-operator-app-operator"}[{{.window}}])-(sum_over_time(up{job=~"kube-prometheus-stack-operator|prometheus-operator-app-operator"}[{{.window}}]))`}}
          totalQuery: |-
            {{`count_over_time(up{job=~"kube-prometheus-stack-operator|prometheus-operator-app-operator"}[{{.window}}])`}}
      alerting:
        name: KubePrometheusStackPrometheusOperatorUnavailable
        labels:
          team: atlas
          area: platform
          topic: observability
        annotations:
          description: {{`kube-prometheus-stack prometheus-operator {{$labels.pod}} in namespace {{ $labels.namespace }} is unavailable.`}}
          opsrecipe: prometheus-operator
        pageAlert:
          disable: false
          labels:
            cancel_if_any_cluster_control_plane_unhealthy: "true"
            cancel_if_cluster_has_no_workers: "true"
            cancel_if_cluster_status_updating: "true"
            cancel_if_outside_working_hours: "true"
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: atlas
        ticketAlert:
          disable: false
          labels:
            severity: "notify"
            slack_channel: "#team-atlas"
