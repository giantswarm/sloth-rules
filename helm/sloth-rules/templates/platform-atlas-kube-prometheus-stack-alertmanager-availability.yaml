# This file is automatically generated. Any manual changes will be wiped out.
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: platform-atlas-kube-prometheus-stack-alertmanager-availability
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: atlas
spec:
  service: "kube-prometheus-stack"
  labels:
    owner: "atlas"
  slos:
    - name: "alertmanager-availability"
      objective: 99
      description: kube-prometheus-stack alertmanager is unavailable
      sli:
        events:
          errorQuery: |-
            {{`count_over_time(up{job=~"prometheus-operator-app-alertmanager|kube-prometheus-stack-alertmanager", container="alertmanager"}[{{.window}}])-(sum_over_time(up{job=~"alertmanager-operated|prometheus-operator-app-alertmanager|kube-prometheus-stack-alertmanager", container="alertmanager"}[{{.window}}]))`}}
          totalQuery: |-
            {{`count_over_time(up{job=~"prometheus-operator-app-alertmanager|kube-prometheus-stack-alertmanager", container="alertmanager"}[{{.window}}])`}}
      alerting:
        name: KubePrometheusStackAlertmanagerUnavailable
        labels:
          team: atlas
          area: platform
          topic: observability
        annotations:
          dashboard: alertmanager-overview/alertmanager-overview
          description: {{`kube-prometheus-stack alertmanager {{$labels.pod}} in namespace {{ $labels.namespace }} is unavailable.`}}
          opsrecipe: service-level-burn-rate-too-high
        pageAlert:
          disable: false
          labels:
            cancel_if_any_cluster_control_plane_unhealthy: "true"
            cancel_if_cluster_has_no_workers: "true"
            cancel_if_cluster_status_updating: "true"
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: atlas
        ticketAlert:
          disable: false
          labels:
            severity: "notify"
            slack_channel: "#team-atlas"
