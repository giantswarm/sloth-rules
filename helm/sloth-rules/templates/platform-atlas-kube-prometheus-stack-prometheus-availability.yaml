# This file is automatically generated. Any manual changes will be wiped out.
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: platform-atlas-kube-prometheus-stack-prometheus-availability
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: atlas
spec:
  service: "kube-prometheus-stack"
  labels:
    owner: "atlas"
  slos:
    - name: "prometheus-availability"
      objective: 99
      description: kube-prometheus-stack prometheus is unavailable
      sli:
        events:
          errorQuery: |-
            {{`count_over_time(up{job=~"kube-prometheus-stack-prometheus|prometheus-operator-app-prometheus", container="prometheus"}[{{.window}}])-(sum_over_time(up{job=~"kube-prometheus-stack-prometheus|prometheus-operator-app-prometheus", container="prometheus"}[{{.window}}]))`}}
          totalQuery: |-
            {{`count_over_time(up{job=~"kube-prometheus-stack-prometheus|prometheus-operator-app-prometheus", container="prometheus"}[{{.window}}])`}}
      alerting:
        name: KubePrometheusStackPrometheusUnavailable
        labels:
          team: atlas
          area: platform
          topic: observability
        annotations:
          description: {{`kube-prometheus-stack prometheus {{$labels.pod}} in namespace {{ $labels.namespace }} is unavailable.`}}
          opsrecipe: service-level-burn-rate-too-high
        pageAlert:
          disable: false
          labels:
            cancel_if_any_cluster_control_plane_unhealthy: "true"
            cancel_if_cluster_has_no_workers: "true"
            cancel_if_cluster_status_updating: "true"
            cancel_if_outside_working_hours: "true"
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: atlas
        ticketAlert:
          disable: false
          labels:
            severity: "notify"
            slack_channel: "#team-atlas"
