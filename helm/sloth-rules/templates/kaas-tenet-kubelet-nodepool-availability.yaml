# This file is automatically generated. Any manual changes will be wiped out.
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-tenet-kubelet-nodepool-availability
  namespace: monitoring
  labels:
    application.giantswarm.io/team: tenet
spec:
  service: "kubelet"
  labels:
    owner: "tenet"
    customer: "{{ .Values.managementCluster.customer }}"
    installation: "{{ .Values.managementCluster.name }}"
    pipeline: "{{ .Values.managementCluster.pipeline }}"
    {{- if .Values.managementCluster.region }}
    region: "{{ .Values.managementCluster.region }}"
    {{- end }}
  slos:
    - name: "nodepool-availability"
      objective: 99
      description: "Kubelet Nodepool availability SLO"
      sli:
        events:
          errorQuery: |-
            {{`label_replace(sum_over_time(kube_node_status_condition{condition="Ready", status!="true"}[{{.window}}]) * on (cluster_id, cluster_type, customer, installation, pipeline, provider, region, node, pod) group_left(nodepool) kube_node_labels{nodepool=~".+"}, "service", "kubelet nodepool $1", "nodepool", "(.*)") and on (cluster_id, cluster_type, customer, installation, pipeline, provider, region, node) kube_node_spec_unschedulable == 0 and on (cluster_id, cluster_type, customer, installation, pipeline, provider, region, node) time() - kube_node_created > 10 * 60`}}
          totalQuery: |-
            {{`label_replace(count_over_time(kube_node_labels{nodepool=~".+"}[{{.window}}]), "service", "kubelet nodepool $1", "nodepool", "(.+)")`}}
      alerting:
        name: "KubeletNodepoolAvailability"
        labels:
          cancel_if_cluster_status_updating: "true"
          cancel_if_monitoring_agent_down: "true"
          cancel_if_outside_working_hours: "true"
          area: kaas
          cluster_id: "{{ printf "{{ .Labels.cluster_id }}" }}"
          cluster_type: "{{ printf "{{ .Labels.cluster_id }}" }}"
          team: {{ include "providerTeam" . }}
        annotations:
          opsrecipe: "service-level-burn-rate-too-high/"
        pageAlert:
          disable: false
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
        ticketAlert:
          disable: false
          labels:
            severity: "notify"
            slack_channel: {{ include "providerTeam" . | replace "'" "" | printf "#alert-%s" | quote }}
