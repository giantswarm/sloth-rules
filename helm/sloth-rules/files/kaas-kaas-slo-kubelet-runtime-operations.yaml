# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-kaas-slo-kubelet-runtime-operations
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: kaas-slo
spec:
  service: "kubelet"
  labels:
    owner: "kaas-slo"
  slos:
    - name: "runtime-operations"
      objective: 99
      description: Kubelet Runtime Operations SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(kubelet_runtime_operations_errors_total[{{.window}}])) by (cluster_id)
          totalQuery: |-
            sum(rate(kubelet_runtime_operations_total[{{.window}}])) by (cluster_id)
      alerting:
        name: KubeletRuntimeErrorRateTooHigh
        labels:
          team: kaas-slo
          area: kaas
        annotations:
          opsrecipe: <no value>
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: kaas-slo
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-kaas-slo"
