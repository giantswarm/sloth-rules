# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-kubelet-pleg-duration
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: phoenix
spec:
  service: "kubelet"
  labels:
    owner: "phoenix"
  slos:
    - name: "pleg-duration"
      objective: 99
      description: Kubelet PLEG Duration SLO
      sli:
        events:
          errorQuery: |-
            clamp_min(sum(rate(kubelet_pleg_relist_duration_seconds_count{role=~"master|worker"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region) - sum(rate(kubelet_pleg_relist_interval_seconds_bucket{le="2.5",role=~"master|worker"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region), 0)
          totalQuery: |-
            sum(rate(kubelet_pleg_relist_duration_seconds_count{role=~"worker|master"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
      alerting:
        name: KubeletPLEGDurationTooHigh
        labels:
          team: phoenix
          area: kaas
        pageAlert:
          labels:
            silence: "true"
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-phoenix"
