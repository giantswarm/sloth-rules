# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-kaas-slo-dns-availability
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: kaas-slo
spec:
  service: "dns"
  labels:
    owner: "kaas-slo"
  slos:
    - name: "availability"
      objective: 99
      description: DNS resolution not available
      sli:
        events:
          errorQuery: |-
            avg(count_over_time(probe_success{target=~"dns-.*"}[{{.window}}])) by (cluster_id) - avg(sum_over_time(probe_success{target=~"dns-.*"}[{{.window}}])) by (cluster_id)
          totalQuery: |-
            avg(count_over_time(probe_success{target=~"dns-.*"}[{{.window}}])) by (cluster_id)
      alerting:
        name: DNSResolutionUnavailable
        labels:
          team: kaas-slo
          area: kaas
        annotations:
          opsrecipe: <no value>
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: kaas-slo
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-kaas-slo"
