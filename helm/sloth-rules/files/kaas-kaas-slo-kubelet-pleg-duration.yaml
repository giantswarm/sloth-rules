# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-kaas-slo-kubelet-pleg-duration
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: kaas-slo
spec:
  service: "kubelet"
  labels:
    owner: "kaas-slo"
  slos:
    - name: "pleg-duration"
      objective: 99
      description: Kubelet PLEG Duration SLO
      sli:
        events:
          errorQuery: |-
            clamp_min(sum(rate(kubelet_pleg_relist_duration_seconds_count{role=~"master|worker"}[{{.window}}])) by (cluster_id) - sum(rate(kubelet_pleg_relist_interval_seconds_bucket{le="2.5",role=~"master|worker"}[{{.window}}])) by (cluster_id), 0)
          totalQuery: |-
            sum(rate(kubelet_pleg_relist_duration_seconds_count{role=~"worker|master"}[{{.window}}])) by (cluster_id)
      alerting:
        name: KubeletPLEGDurationTooHigh
        labels:
          team: kaas-slo
          area: kaas
        annotations:
          opsrecipe: <no value>
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: kaas-slo
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-kaas-slo"
