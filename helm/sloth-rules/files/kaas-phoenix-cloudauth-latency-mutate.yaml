# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-cloudauth-latency-mutate
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: phoenix
spec:
  service: "cloudauth"
  labels:
    owner: "phoenix"
  slos:
    - name: "latency-mutate"
      objective: 99
      description: AWS Pod Identity Webhook Mutate Latency
      sli:
        events:
          errorQuery: |-
            sum(rate(http_request_latencies_count{app="aws-pod-identity-webhook"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region) - sum(rate(http_request_latencies_bucket{app="aws-pod-identity-webhook", le="250000"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
          totalQuery: |-
            sum(rate(http_request_latencies_count{app="aws-pod-identity-webhook"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
      alerting:
        name: AWSPodIdentityWebhookLatencyTooHigh
        labels:
          team: phoenix
          area: kaas
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-phoenix"
