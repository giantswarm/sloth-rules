# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: platform-shield-starboard-exporter-starboard-exporter-errors
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: shield
spec:
  service: "starboard-exporter"
  labels:
    owner: "shield"
  slos:
    - name: "starboard-exporter-errors"
      objective: 99
      description: Starboard Exporter error rate measured by workqueue stats
      sli:
        events:
          errorQuery: |-
            sum(rate(workqueue_retries_total{job="starboard-exporter",name="vulnerabilityreport"}[{{.window}}])) by (instance)
          totalQuery: |-
            sum(rate(workqueue_adds_total{job="starboard-exporter",name="vulnerabilityreport"}[{{.window}}])) by (instance)
      alerting:
        name: StarboardExporterErrorRate
        labels:
          team: shield
          area: platform
        annotations: {}
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: shield
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-shield"
