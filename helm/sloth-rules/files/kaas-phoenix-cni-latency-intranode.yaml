# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-cni-latency-intranode
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: phoenix
spec:
  service: "cni"
  labels:
    owner: "phoenix"
  slos:
    - name: "latency-intranode"
      objective: 99
      description: Cilium node connectivity latency
      sli:
        events:
          errorQuery: |-
            avg(quantile_over_time(0.99, cilium_node_connectivity_latency_seconds{target_node_type="remote_intra_cluster"}[{{.window}}])) by (cluster_id) > 0.001
          totalQuery: |-
            avg(quantile_over_time(0.99, cilium_node_connectivity_latency_seconds{target_node_type="remote_intra_cluster"}[{{.window}}])) by (cluster_id)
      alerting:
        name: CiliumNodeConnectivityLatencyTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations:
          opsrecipe: <no value>
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-phoenix"
