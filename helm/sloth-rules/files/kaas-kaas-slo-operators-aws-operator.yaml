# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-kaas-slo-operators-aws-operator
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: kaas-slo
spec:
  service: "operators"
  labels:
    owner: "kaas-slo"
  slos:
    - name: "aws-operator"
      objective: 99
      description: AWSOperator SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(operatorkit_controller_errors_total{container="aws-operator"}[{{.window}}])) by (app)
          totalQuery: |-
            sum(rate(operatorkit_controller_operation_total{container="aws-operator"}[{{.window}}])) by (app)
      alerting:
        name: AWSOperatorErrorRateTooHigh
        labels:
          team: kaas-slo
          area: kaas
        annotations:
          opsrecipe: <no value>
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: kaas-slo
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-kaas-slo"
