# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-containerd-containerd-disk-usage
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: phoenix
spec:
  service: "containerd"
  labels:
    owner: "phoenix"
  slos:
    - name: "containerd-disk-usage"
      objective: 10
      description: Containerd Disk Usage
      sli:
        events:
          errorQuery: |-
            sum_over_time(node_filesystem_free_bytes{mountpoint=~"(/rootfs)?/var/lib/containerd"}[{{.window}}])
          totalQuery: |-
            sum_over_time(node_filesystem_size_bytes{mountpoint=~"(/rootfs)?/var/lib/containerd"}[{{.window}}])
      alerting:
        name: ContainerdDiskUsage
        labels:
          team: phoenix
          area: kaas
        annotations:
          opsrecipe: low-disk-space/#containerd-volume
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-phoenix"
