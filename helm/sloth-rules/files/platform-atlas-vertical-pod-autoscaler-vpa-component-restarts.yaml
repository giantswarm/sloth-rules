# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: platform-atlas-vertical-pod-autoscaler-vpa-component-restarts
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: atlas
spec:
  service: "vertical-pod-autoscaler"
  labels:
    owner: "atlas"
  slos:
    - name: "vpa-component-restarts"
      objective: 99
      description: VPA's component(s) has restarted several times
      sli:
        events:
          errorQuery: |-
            count(kube_pod_container_status_ready{container=~"recommender|updater|admission-controller", pod=~"vertical-pod-autoscaler-app-recommender-.*|vertical-pod-autoscaler-app-updater-.*|vertical-pod-autoscaler-app-admission-controller-.*"} == 0) by (container)
          totalQuery: |-
            count(kube_pod_container_status_ready{container=~"recommender|updater|admission-controller", pod=~"vertical-pod-autoscaler-app-recommender-.*|vertical-pod-autoscaler-app-updater-.*|vertical-pod-autoscaler-app-admission-controller-.*"}) by (container)
      alerting:
        name: VpaComponentTooManyRestarts
        labels:
          team: atlas
          area: platform
        annotations: {}
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: atlas
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-atlas"
