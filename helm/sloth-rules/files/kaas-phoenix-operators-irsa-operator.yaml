# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-operators-irsa-operator
  namespace: monitoring
  labels:
    release: prometheus
    application.giantswarm.io/team: phoenix
spec:
  service: "operators"
  labels:
    owner: "phoenix"
  slos:
    - name: "irsa-operator"
      objective: 99
      description: IRSA Operator SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(controller_runtime_reconcile_errors_total{app="irsa-operator"}[{{.window}}]))
          totalQuery: |-
            sum(rate(controller_runtime_reconcile_total{app="irsa-operator"}[{{.window}}]))
      alerting:
        name: IRSAOperatorErrorRateTooHigh
        labels:
          team: phoenix
          area: kaas
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "notify"
            slack_channel: "#team-phoenix"
