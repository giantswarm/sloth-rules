# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-cloudauth-latency-mutate
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "cloudauth"
  labels:
    owner: "phoenix"
  slos:
    - name: "latency-mutate"
      objective: 99
      description: AWS Pod Identity Webhook Mutate Latency
      sli:
        events:
          errorQuery: |-
            sum(rate(http_request_latencies_count{app="aws-pod-identity-webhook"}[{{.window}}])) by (cluster_id) - sum(rate(http_request_latencies_bucket{app="aws-pod-identity-webhook", le="250000"}[{{.window}}])) by (cluster_id)
          totalQuery: |-
            sum(rate(http_request_latencies_count{app="aws-pod-identity-webhook"}[{{.window}}])) by (cluster_id)
      alerting:
        name: AWSPodIdentityWebhookLatencyTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
