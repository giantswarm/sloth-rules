# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-apiserver-kubelet-runtime-operations
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "apiserver"
  labels:
    owner: "phoenix"
  slos:
    - name: "kubelet-runtime-operations"
      objective: 99
      description: Kubelet Runtime Operations SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(kubelet_runtime_operations_errors_total[{{.window}}])) by (operation_type, cluster_id)
          totalQuery: |-
            sum(rate(kubelet_runtime_operations_total[{{.window}}])) by (operation_type, cluster_id)
      alerting:
        name: KubeletRuntimeErrorRateTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
