# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-cni-latency-endpoint
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "cni"
  labels:
    owner: "phoenix"
  slos:
    - name: "latency-endpoint"
      objective: 99
      description: Cilium endpoint regeneration latency
      sli:
        events:
          errorQuery: |-
            sum(rate(cilium_endpoint_regeneration_time_stats_seconds_count{scope="total"}[{{.window}}])) by (cluster_id) - sum(rate(cilium_endpoint_regeneration_time_stats_seconds_bucket{scope="total", le="0.05"}[{{.window}}])) by (cluster_id)
          totalQuery: |-
            sum(rate(cilium_endpoint_regeneration_time_stats_seconds_count{scope="total"}[{{.window}}])) by (cluster_id)
      alerting:
        name: CiliumEndpointRegenerationLatencyTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
