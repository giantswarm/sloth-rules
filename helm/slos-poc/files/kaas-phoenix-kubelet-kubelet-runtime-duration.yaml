# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-kubelet-kubelet-runtime-duration
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "kubelet"
  labels:
    owner: "phoenix"
  slos:
    - name: "kubelet-runtime-duration"
      objective: 99
      description: Kubelet Operations Duration SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(kubelet_pod_worker_duration_seconds_count{operation_type=~"create|update"}[{{.window}}])) by (cluster_id) - sum(rate(kubelet_pod_worker_duration_seconds_bucket{le="1",operation_type=~"create|update"}[{{.window}}])) by (cluster_id)
          totalQuery: |-
            sum(rate(kubelet_pod_worker_duration_seconds_count{operation_type=~"create|update"}[{{.window}}])) by (cluster_id)
      alerting:
        name: KubeletRuntimeDurationTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
