# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-admission-aws-admission-controller-latancy
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "admission"
  labels:
    owner: "phoenix"
  slos:
    - name: "aws-admission-controller-latancy"
      objective: 99
      description: AWS Admission Controller Latency
      sli:
        events:
          errorQuery: |-
            clamp_min(sum(rate(apiserver_admission_webhook_admission_duration_seconds_count{name=~".+aws-admission-controller.giantswarm.io"}[{{.window}}])) by (cluster_id) - sum(rate(apiserver_admission_webhook_admission_duration_seconds_bucket{le="0.5",name=~".+aws-admission-controller.giantswarm.io"}[{{.window}}])) by (cluster_id), 0)
          totalQuery: |-
            sum(rate(apiserver_admission_webhook_admission_duration_seconds_count{name=~".+aws-admission-controller.giantswarm.io"}[{{.window}}])) by (cluster_id)
      alerting:
        name: AWSAdmissionControllerLatencyTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            cancel_if_cluster_status_creating: "true"
            cancel_if_cluster_status_deleting: "true"
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
