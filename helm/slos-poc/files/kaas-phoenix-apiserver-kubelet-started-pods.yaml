# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-apiserver-kubelet-started-pods
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "apiserver"
  labels:
    owner: "phoenix"
  slos:
    - name: "kubelet-started-pods"
      objective: 99
      description: Kubelet Started Pods SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(kubelet_started_pods_errors_total[{{.window}}])) by (cluster_id)
          totalQuery: |-
            sum(rate(kubelet_started_pods_total[{{.window}}])) by (cluster_id)
      alerting:
        name: KubeletStartedPodsErrorRateTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
