# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-kubelet-kubelet-pleg-duration
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "kubelet"
  labels:
    owner: "phoenix"
  slos:
    - name: "kubelet-pleg-duration"
      objective: 99
      description: Kubelet PLEG Duration SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(kubelet_pleg_relist_duration_seconds_count{role=~"worker|master"}[{{.window}}])) by (cluster_id,le) > 10
          totalQuery: |-
            sum(rate(kubelet_pleg_relist_duration_seconds_count{role=~"worker|master"}[{{.window}}])) by (cluster_id,le) <= 10
      alerting:
        name: KubeletPLEGDurationTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
