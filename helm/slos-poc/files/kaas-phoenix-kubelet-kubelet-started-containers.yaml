# This file is automatically generated. Any manual changes will be wiped out.
---
apiVersion: sloth.slok.dev/v1
kind: PrometheusServiceLevel
metadata:
  name: kaas-phoenix-kubelet-kubelet-started-containers
  namespace: monitoring
  labels:
    release: prometheus
spec:
  service: "kubelet"
  labels:
    owner: "phoenix"
  slos:
    - name: "kubelet-started-containers"
      objective: 99
      description: Kubelet Started Containers SLO
      sli:
        events:
          errorQuery: |-
            sum(rate(kubelet_started_containers_errors_total[{{.window}}])) by (cluster_id)
          totalQuery: |-
            sum(rate(kubelet_started_containers_total[{{.window}}])) by (cluster_id)
      alerting:
        name: KubeletStartedContainersErrorRateTooHigh
        labels:
          team: phoenix
          area: kaas
        annotations: {}
        pageAlert:
          labels:
            severity: page
            team: phoenix
        ticketAlert:
          labels:
            severity: "slack"
            slack_channel: "#team-phoenix"
