---
description: "DNS service not available"
alertName: "DNSServiceUnavailable"
objective: 99
errorQuery: '{{`sum(rate(coredns_dns_responses_total{rcode="SERVFAIL"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region, app)`}}'
totalQuery: '{{`sum(rate(coredns_dns_responses_total[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region, app)`}}'
annotations:
  runbook_url: https://intranet.giantswarm.io/docs/support-and-ops/ops-recipes/dns-issue-mitigation
  __dashboardUid__: Yu9tkufmk
  dashboardQueryParams: orgId=1
alertLabels:
  cancel_if_outside_working_hours: '{{ include "workingHoursOnly" . }}'
