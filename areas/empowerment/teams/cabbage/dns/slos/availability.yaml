---
description: "DNS resolution not available"
alertName: "DNSResolutionUnavailable"
objective: 99
errorQuery: avg(count_over_time(probe_success{target=~"dns-.*"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region) - avg(sum_over_time(probe_success{target=~"dns-.*"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
totalQuery: avg(count_over_time(probe_success{target=~"dns-.*"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
annotations:
  opsrecipe: dns-issue-mitigation
alertLabels:
  cancel_if_outside_working_hours: "true"
