---
description: "DNS resolution latency from Node Local DNS Cache"
alertName: "LocalDNSCacheLatencyTooHigh"
objective: 99
errorQuery: sum(rate(coredns_dns_request_duration_seconds_count{app="k8s-dns-node-cache"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region) - sum(rate(coredns_dns_request_duration_seconds_bucket{app="k8s-dns-node-cache",le="0.008"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
totalQuery: sum(rate(coredns_dns_request_duration_seconds_count{app="k8s-dns-node-cache"}[{{.window}}])) by (cluster_id, cluster_type, customer, installation, pipeline, provider, region)
annotations:
  opsrecipe: dns-issue-mitigation
alertLabels:
  cancel_if_outside_working_hours: "true"
